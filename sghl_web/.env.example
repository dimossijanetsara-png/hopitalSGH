# ================================================================
#  SGHL Web — Variables d'environnement Vite
#  DEV  : copiez en .env.local  (ignoré par git)
#  PROD : définissez dans Render > Environment (ou render.yaml)
# ================================================================

# URL de l'API Django backend
# Dev local :
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Production Render :
# VITE_API_BASE_URL=https://sghl-backend.onrender.com/api/v1
